function Navbar({ className }) {
    return (
        <section className={`flex ${className}`}>
            <h1>My Blog</h1>
            <h2>About</h2>
        </section>
    );
}

export default Navbar;
